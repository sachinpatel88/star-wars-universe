query PagedPeople($cursor: Int) {
    people(cursor: $cursor) {
        people {
            name
            gender
            url
        }
        cursor {
            nextPage
            prevPage
        }
    }
}
